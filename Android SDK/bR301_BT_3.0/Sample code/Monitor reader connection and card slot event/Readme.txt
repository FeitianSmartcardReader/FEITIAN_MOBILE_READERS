Monitor reader status:
	//#1, register card status monitoring
		BlueToothReceiver.registerCardStatusMonitoring(mHandler);
	//#2, handle message of bluetooth		handleMessage(Message msg)

Monitor card slot status:
	//#1 register card status monitoring registerCardStatusMonitoring(Handler Handler)
